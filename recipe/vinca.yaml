# meta.yaml root package name
name: ros-eloquent-fat-desktop

ros_distro: eloquent

fat_archive: true

# mapping for package keys
conda_index:
  - conda-forge.yaml
  - packages-ignore.yaml

packages_skip_by_deps:

packages_select_by_deps:
  - desktop

patch_dir: patch
